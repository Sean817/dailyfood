version: '3.8'

services:
  dailyfood:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: dailyfood-app
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production

